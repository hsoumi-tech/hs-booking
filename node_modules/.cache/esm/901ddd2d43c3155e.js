let mongoose,dbUrl,logger;_43c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_43c‍.w("./helper",[["dbUrl",["dbUrl"],function(v){dbUrl=v}]]);_43c‍.w("./logger",[["default",["logger"],function(v){logger=v}]]);




mongoose.connection.on('connected', () => {
  logger.info(`Database successfully connected to ${dbUrl}`);
});

mongoose.connection.on('reconnected', () => {
  logger.info(`Database successfully reconnected to ${dbUrl}`);
});

mongoose.connection.on('disconnected', () => {
  logger.info('Connection Disconnected');
});

mongoose.connection.on('close', () => {
  logger.info('Connection Closed');
});

mongoose.connection.on('error', error => {
  logger.error(error);
});

const run = async () => {
  await mongoose.connect(dbUrl, {
    autoReconnect: true,
    reconnectTries: 1000000,
    reconnectInterval: 3000
  });
};

run();
