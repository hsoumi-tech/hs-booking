let getAllBedTypes,addBedType,getBedTypeById,updateBedType,deleteBedType;_564‍.x([["default",()=>_564‍.o]]);_564‍.w("../../controllers/hotel",[["getAllBedTypes",["getAllBedTypes"],function(v){getAllBedTypes=v}],["addBedType",["addBedType"],function(v){addBedType=v}],["getBedTypeById",["getBedTypeById"],function(v){getBedTypeById=v}],["updateBedType",["updateBedType"],function(v){updateBedType=v}],["deleteBedType",["deleteBedType"],function(v){deleteBedType=v}]]);







const _564‍anonymous=(fastify, opts, next) => {

  // get all bed type 
  fastify.get(
    '/bed-type',
    async () => {
      const result = await getAllBedTypes();
      return result;
    }
  );

  // get bed type by id
  fastify.get(
    '/bed-type/:id',
    async req => {
      const result = await getBedTypeById(req.params.id);
      return result;
    }
  );

  // add bed type 
  const addBedTypeSchema = {
    body: {
      type: 'object',
      required: ['name', 'description', 'size'],
      properties: {
        name: {
          type: 'string'
        },
        description: {
          type: 'string'
        },
        size: {
          type: 'number',
          minimum: 1
        }
      }
    }
  };

  fastify.post(
    '/bed-type', {
      schema: addBedTypeSchema
    },
    async req => {
      const result = await addBedType(req.body);
      return result;
    }
  );

  // update bed type 
  const updateBedTypeSchema = {
    body: {
      type: 'object',
      required: [],
      properties: {
        name: {
          type: 'string'
        },
        description: {
          type: 'string'
        },
        size: {
          type: 'number',
          minimum: 1
        }
      }
    }
  };

  fastify.put(
    '/bed-type/:id', {
      schema: updateBedTypeSchema
    },
    async req => {
      const result = await updateBedType(Object.assign({}, req.body, {
        id: req.params.id
      }));
      return result;
    }
  );

  // delete bed type 
  fastify.delete(
    '/bed-type/:id',
    async req => {
      const result = await deleteBedType(req.params.id);
      return result;
    }
  );
  next();
};_564‍.d(_564‍anonymous);